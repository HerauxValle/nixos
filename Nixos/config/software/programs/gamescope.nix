# &desc: "gamescope program config -- enabled, micro-compositor for FSR upscaling / frame limiting / fullscreen fixes via `gamescope ... -- %command%`."

{ ... }:

{
  config.vars.packages.programs.gamescope.enable = true;
}
