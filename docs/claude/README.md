# docs/claude/

&desc: "Index of session continuation docs for Claude -- check here first when resuming work that spans multiple chats."

Session-continuation notes for work that's too deep/expensive to re-derive from scratch in a
fresh chat. Read the relevant file **before** touching the related code.

- [canvas-continuation.md](canvas-continuation.md) — the `Hyprland/plugins/canvas` infinite
  canvas plugin: full bug log, current state, exact next steps. Read this first if asked to work
  on the canvas plugin again.
