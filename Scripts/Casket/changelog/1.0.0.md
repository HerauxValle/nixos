<!-- &desc: "v1.0.0 changelog." -->
# 1.0.0

Baseline. Rust rewrite of the original Python `cas` (`.old/main.py`).
Same on-disk format, same CLI shape. Starting point for this changelog —
earlier history isn't reconstructed here.
