<!-- &desc: "v1.4.1 changelog." -->
# 1.4.1

- fixed: `settings encryption`/`2fa` and `auth keyfile reset`/`activate`
  would prompt for the passphrase twice when verification was required
  for them — once for the verification check, once for their own
  operation. Now resolved once and reused.
