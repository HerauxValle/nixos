<!-- &desc: "v1.10.10 changelog." -->
# 1.10.10

- added: `src/registry/rootfs.rs` — parses `data/rootfs-presets.toml`
  (alpine/ubuntu/debian starter set) and resolves `{arch}`/`{version}`/
  `{minor}` placeholders into concrete URLs. Offline/pure, table-driven
  tests so a new preset entry gets structural coverage automatically.
  Not wired into any command yet — `rootfs add --preset` (live fetch +
  checksum verify) is the next slice.
