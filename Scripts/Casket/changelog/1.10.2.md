<!-- &desc: "v1.10.2 changelog." -->
# 1.10.2

- changed: `Meta`/`tamper::Protected` gained the sandbox data model
  (`sandbox_enabled`, `sandbox_namespaces`, `sandbox_seccomp`,
  `sandbox_seccomp_custom_hash`, cgroup limits, backup-rootfs include
  set) — groundwork for the upcoming `cas exec` sandbox feature, no
  new command or flag yet
