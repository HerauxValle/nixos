<!-- &desc: "v1.3.0 changelog." -->
# 1.3.0

- added: `delete --removeKeyfile`.
- changed: `delete` no longer deletes the vault's keyfile by default —
  a keyfile isn't necessarily exclusive to one vault, so removing it
  automatically was riskier than it looked. Pass `--removeKeyfile` to
  get the old behavior back.
