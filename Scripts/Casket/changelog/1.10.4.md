<!-- &desc: "v1.10.4 changelog." -->
# 1.10.4

- added: `settings security sandbox enable|disable|state` — top-level
  toggle for the upcoming `cas exec` sandbox feature. Verification-
  gated and tamper-HMAC-covered like the other protection toggles.
- fixed: `tampered`/`open`'s tamper-check messages no longer hardcode
  a stale list of protected field names (now generic, stays accurate
  as the protected set grows)
